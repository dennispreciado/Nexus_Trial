{
  "name": "pwjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pretest": "tsc --incremental -p tests/tsconfig.json",
    "tests": "playwright test --retries 3",
    "smoke": "playwright test --grep smoke",
    "debug": "npx playwright test --debug"
  },
  "keywords": [],
  "author": "Dennis Preciado",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.28.0"
  },
  "dependencies": {
    "@faker-js/faker": "^7.6.0",
    "jwt-decode": "^3.1.2"
  }
}
